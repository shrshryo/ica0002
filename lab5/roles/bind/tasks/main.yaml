---
 - name: install bind9
   become: yes
   apt:
     install: bind9
     state: latest

 - name: ensure that bind9 is enabled
   become: yes
   service
     name: bind
     enabled: yes

 - name: ensure that bind9 is started
   become: yes
   service:
     name: bind
     state: started