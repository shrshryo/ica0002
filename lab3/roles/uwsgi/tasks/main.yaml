---
  - name: install uwsgi
    become: yes
    apt:
      name: uwsgi
    state: latest

  - name: install uwsgi-plugin-python3
    become: yes
    apt:
      name: uwsgi-plugin-python3
    state: latest

 - name: add uwsgi config file
   copy:
     src: ansible.cfg
     dst: /etc/uwsgi/apps-enabled/agama.ini
   notify: restart uwsgi
